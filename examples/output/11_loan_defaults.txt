== fincurve report: multi-feature mode (default vs 3 features) ==
[Data profile]
  - 4000 observations; y: binary 0/1 outcome
  - features: 2 numeric, 1 removed
[Automatic choices]
  - error model: proportions (binomial) — y takes only the values 0 and 1: Bernoulli likelihood
  - validation: shuffled K-fold, 5 splits — no time ordering
  - shape search: each numeric feature chooses among linear, quadratic, cubic, log, square root, reciprocal, power, exponential, sigmoid, peak and hinge; a shape is accepted only if AIC drops by at least 4, and an interaction only if AIC drops by at least 10
  - the shape search is repeated inside every fold (nested), so the score is not flattered by it
[Ranking]  ΔCV-NLL = held-out negative log-likelihood per row relative to the best model;
           * recommended, = practically tied with the best (within 2 paired SE and 0.1 nats)
* 1. Additive shape model                        k= 5  ΔCV-NLL=        0 ±0        CV-RMSE=   0.367  ΔAIC=    0.0
  2. All-linear GLM (baseline)                   k= 3  ΔCV-NLL=   0.0102 ±0.0026   CV-RMSE=   0.371  ΔAIC=   80.7
  3. k-nearest neighbours (reference)            k= -  ΔCV-NLL=reference ±0.011    CV-RMSE=       -
[Recommended model: Additive shape model]
  - debt_to_income [numeric]: S-shaped rise (sigmoid (threshold)), importance 85%, fold-to-fold curve correlation 1.00
  - utilization [numeric]: roughly linear rise (linear), importance 15%, fold-to-fold curve correlation 1.00
  - row_number: removed — consecutive unique integers: looks like a row number or ID and would leak
[Warnings]
  - feature 'row_number' was removed: consecutive unique integers: looks like a row number or ID and would leak
[Notes]
  - 'debt_to_income': linear → sigmoid (threshold) (full-sample AIC −80.7)

(elapsed 0.9s)
